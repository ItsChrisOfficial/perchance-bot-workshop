---
applyTo: "bots/**"
---

Keep bots organized by lifecycle: `completed`, `in-progress`, `templates`.
Use one folder per bot with predictable names in kebab-case.
Avoid embedding duplicate helper code across bot projects.
Move finished, validated bots from `in-progress` to `completed`.
Update `BOT_CATALOG.md` after adding, moving, or retiring bots.
